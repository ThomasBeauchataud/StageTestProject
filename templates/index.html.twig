{% extends "base.html.twig" %}
{% block body %}
    {% if message is defined %}
        <div style="color: green; font-size: 30px; text-align: center; padding-bottom: 20px">
            {{ message }}
        </div>
    {% endif %}
    <div style="text-align: center; font-size: 40px;">Welcome on my website</div>
{% endblock %}